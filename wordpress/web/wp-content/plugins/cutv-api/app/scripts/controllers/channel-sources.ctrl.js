'use strict';

/**
 * @ngdoc function
 * @name cutvApiAdminApp.controller:ChannelSourceCtrl
 * @description
 * # MainCtrl
 * Controller of the cutvApiAdminApp
 */
angular.module('cutvApiAdminApp')

    .controller('ChannelSourceCtrl', function ($scope, $http, $location, $routeParams, ChannelService) {

        // init
        // $scope.channelName = $routeParams.channelName;
        // $scope.channelId = $routeParams.channelId;
        
        // // $scope.sources = await ChannelService.getSources($scope.channelId).then(sources => sources);

        // // $scope.channel = cutv.channels.find(x => x.pid === $scope.channelId);
        debugger;

        // (function initialize() {

        //     // ChannelService.getSources($scope.channelId).then((sources) => {
        //     //     debugger;
        //     //     // init
        //     //     $scope.channelName = $routeParams.channelName;
        //     //     $scope.channelId = $routeParams.channelId;
        //     //     $scope.sources = sources;
        //     // });
        // })();

    });
